# Railway/Nixpacks: cap JVM heap so Spring Boot fits in small containers (512 MB tier).
[variables]
JAVA_TOOL_OPTIONS = "-XX:MaxRAMPercentage=75.0 -XX:+UseContainerSupport -XX:+ExitOnOutOfMemoryError"

[start]
cmd = "java -jar target/observerr-0.0.1-SNAPSHOT.jar"
